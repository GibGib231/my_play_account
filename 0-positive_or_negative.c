#include<stdio.h>
int main(void)
{
int num1 = 0;

if (num1 < 0)
	// just make a few changese in the code 
	// so it's updated now
	// this is to understand the if, else if and else condition
		{
		printf("%d that is not a positive number\n", num1);
		}
else if(num1 > 0)
{

	printf("%d is a positive number\n but greater then zero in this code\n", num1);
}
else
{
	printf("%d is equal to zero \n", num1);
}

}
